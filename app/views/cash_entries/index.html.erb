<div class="container">
  <div class="bg-light p-3 my-4 rounded ">
    <h2 class='text-center text-uppercase'> Total</h2>
    <hr>
    <div class="d-flex align-items-center justify-space-evenly">
      <div class="text-center alert alert-success">
        <h4 class="ml-3 p-1 text-uppercase">Cash In</h4>
        <h4 class="ml-3 p-1 text-success"><%= CashEntry.total_cash_in %></h4>
      </div>
      <div class="text-center alert alert-danger">
        <h4 class="ml-3 p-1 text-uppercase">Cash Out</h4>
        <h4 class="ml-3 p-1 text-danger"><%= CashEntry.total_cash_out %></h4>
      </div>
      <div class="text-center alert alert-success">
        <h4 class="ml-3 p-1 text-uppercase">Balance</h4>
        <h4 class="ml-3 p-1 <%= balance_text_color(CashEntry.total_balance)%>"><%= CashEntry.total_balance %></h4>
      </div>
    </div>
  </div>

  <%= link_to 'New Cash Entry', new_cash_entry_path, class: 'btn btn-primary' %>

  <hr>
  <h1>Cash Entries</h1>

  <table class="table table-striped table-hover table-bordered table-dark text-center">
    <thead>
      <tr>
        <th>AMOUNT</th>
        <th>DESCRIPTION</th>
        <th>TYPE</th>
        <th>DATE</th>
        <th colspan="3">ACTIONS</th>
      </tr>
    </thead>

    <tbody>
      <% @cash_entries.each do |cash_entry| %>
      <tr>
        <td><%= cash_entry.amount %></td>
        <td><%= cash_entry.description %></td>
        <td><%= cash_entry.entry_type.titleize %></td>
        <td><%= cash_entry.date %></td>
        <td><%= link_to 'Show', cash_entry , class: 'nav-link'%></td>
        <td><%= link_to 'Edit', edit_cash_entry_path(cash_entry) , class: 'nav-link'%></td>
        <td><%= link_to 'Destroy', cash_entry, method: :delete, data: { confirm: 'Are you sure?' } , class: 'nav-link'%></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <br>
</div>